# yaml-language-server: $schema=https://ifstate.net/schema/ifstate.conf.schema.json
#ignore:
#  ifname:
#    - .*
interfaces:
  - name: eno1
    link:
      state: up
      kind: physical

  - name: bond0
    link:
      state: up
      kind: physical
      bond_mode: foo
